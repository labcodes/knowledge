version: '3'

volumes:
  knowledge_static: {}
  knowledge_db: {}

services:
  web:
    restart: always
    image: labcodes/knowledge
    build:
      context: .
      dockerfile: ./conf/django/dev/Dockerfile
    links:
      - postgres
    volumes:
      - .:/app
      - knowledge_static:/app/staticfiles
    env_file: .env_docker
    ports:
      - 8080:8000
    depends_on:
      - postgres

  postgres:
    restart: always
    container_name: knowledge_db
    image: postgres:9.6.5
    volumes:
      - knowledge_db:/var/lib/postgresql/data
    env_file: .env_docker
    ports:
      - 5434:5432
